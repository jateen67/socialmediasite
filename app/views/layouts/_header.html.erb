<nav class="navbar navbar-expand-md navbar-dark bg-dark p-3">
    <%= link_to 'Cloneddit', root_path, :class => "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
            <%= link_to current_user.username, user_path(current_user), :class => 'nav-link mt-2' %>
            <%= link_to edit_user_registration_path, class: 'nav-link mt-2' do %>
              <i class="fa-solid fa-gear"></i>
              Settings
            <% end %>
            <li class='nav-item'>
              <a class='nav-link'><%= button_to raw("<i class=\"fa fa-solid fa-right-from-bracket\"></i> Logout"), destroy_user_session_path, method: :delete, :class => 'btn btn-dark border border-dark border-3 logout' %></a>
            </li>
        <% else %>
              <%= link_to new_user_session_path, class: "nav-link login mt-2 mb-2" do %>
                <i class="fa-solid fa-right-to-bracket"></i>
                Login&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
              <% end %>            
        <% end %>
    </ul>
  </div>
</nav>